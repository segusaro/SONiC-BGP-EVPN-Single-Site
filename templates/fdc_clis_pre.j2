#jinja2: trim_blocks: True, lstrip_blocks: True
{#######################################

Purpose:
Custom configuration for SONiC devices
#########################################}
{%if lacp_pcs is defined and lacp_pcs%}
{%for lacp_pc in lacp_pcs%}
interface PortChannel {{lacp_pc}} mode active fallback
{%endfor%}
{%endif%}